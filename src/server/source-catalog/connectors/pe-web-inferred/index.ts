/**
 * pe-web-inferred — Public API
 *
 * Adapter lógico de discovery para Perú con sector inferido vía web/IA.
 *
 * Exporta la función dry-run y metadatos helpers.
 */

export { runPeWebInferredDryRun, buildEmptyMetadata } from './run-pe-web-inferred-dry-run';
export * from './types';
